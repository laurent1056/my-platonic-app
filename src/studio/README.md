# Local editorial studio

Files in this directory are not public routes.

`OracleStudio.astro` preserves the retired browser-direct Oracle implementation as migration reference. Do not import it from `src/pages/`. The replacement local studio will consume the canonical Constitution and case-file schema, keep credentials in the local environment, and emit drafts that require human review before any Git commit.
